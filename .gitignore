# Office lock files
.~lock.*

# OS
Thumbs.db
.DS_Store
desktop.ini

# Editor
.vscode/
.idea/

# Claude local settings (keep settings.json shared, ignore local overrides)
.claude/settings.local.json

# Claude Code worktrees (were committed as broken gitlinks — never track)
.claude/worktrees/
